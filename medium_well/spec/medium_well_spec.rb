require 'spec_helper'

describe MediumWell do
  it 'has a version number' do
    expect(MediumWell::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
